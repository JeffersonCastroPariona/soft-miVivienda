package com.upc.ep.repositories;

import com.upc.ep.entities.PlanPago;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanPagoRepos extends JpaRepository<PlanPago, Long> {
}
